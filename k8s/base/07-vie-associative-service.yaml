# Documents d'adhésion / certificats médicaux uploadés — PVC dédié, comme
# `backend-uploads-pvc` pour le monolithe (voir 03-monolith.yaml).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vie-associative-uploads-pvc
  namespace: plongee-app
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
# PVC séparé (pas un sous-dossier de vie-associative-uploads-pvc) pour les
# certificats médicaux chiffrés (exigence 4.4) : jamais monté sous
# /app/uploads, donc jamais atteignable par la route express.static — une
# erreur de config ne peut pas accidentellement les rendre publics.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vie-associative-secure-uploads-pvc
  namespace: plongee-app
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vie-associative-service
  namespace: plongee-app
spec:
  replicas: 1
  selector:
    matchLabels: { app: vie-associative-service }
  template:
    metadata:
      labels: { app: vie-associative-service }
      annotations: { prometheus.io/scrape: "true", prometheus.io/port: "5013" }
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: vie-associative-service
          # Référence littérale de secours (kubectl apply -f direct, sans
          # kustomize) — en usage normal, k8s/kustomization.yaml réécrit ce
          # champ vers ghcr.io/.../vie-associative-service:sha-<shortsha> à
          # chaque déploiement GitOps (voir DEPLOY.md).
          image: ghcr.io/gabriel15091509/plong--aquamarine/vie-associative-service:main
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5013
          env:
            - name: DB_SCHEMA
              value: vie_associative
          envFrom:
            - configMapRef: { name: plongee-config }
            - secretRef: { name: plongee-secrets }
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
            - name: secure-uploads
              mountPath: /app/secure-uploads
          readinessProbe:
            httpGet: { path: /health, port: 5013 }
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: uploads
          persistentVolumeClaim: { claimName: vie-associative-uploads-pvc }
        - name: secure-uploads
          persistentVolumeClaim: { claimName: vie-associative-secure-uploads-pvc }
---
apiVersion: v1
kind: Service
metadata:
  name: vie-associative-service
  namespace: plongee-app
spec:
  selector: { app: vie-associative-service }
  ports:
    - port: 5013
      targetPort: 5013
