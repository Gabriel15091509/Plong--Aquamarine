apiVersion: apps/v1
kind: Deployment
metadata:
  name: formation-service
  namespace: plongee-app
spec:
  replicas: 1
  selector:
    matchLabels: { app: formation-service }
  template:
    metadata:
      labels: { app: formation-service }
    spec:
      containers:
        - name: formation-service
          # Référence littérale de secours (kubectl apply -f direct, sans
          # kustomize) — en usage normal, k8s/kustomization.yaml réécrit ce
          # champ vers ghcr.io/.../formation-service:sha-<shortsha> à chaque
          # déploiement GitOps (voir DEPLOY.md).
          image: ghcr.io/gabriel15091509/plong--aquamarine/formation-service:main
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5010
          env:
            - name: DB_SCHEMA
              value: formation
          envFrom:
            - configMapRef: { name: plongee-config }
            - secretRef: { name: plongee-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 5010 }
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: formation-service
  namespace: plongee-app
spec:
  selector: { app: formation-service }
  ports:
    - port: 5010
      targetPort: 5010
